1. Текста не должны все говорит на прямую, лучше не `vendor balance is not numeric, а что то вроде typing error
   Так мы не в лоб говорим что за ошибка, а аккуратно намекам.

2. реализуй красивую архитуектуру внутри папки internal, так например котроллеры добавить в controllers/ и тд

3. Мне пока не понятен хэлс чек с VENDOR_API_URL, он используется один раз и очень не логично, что бы я сделал

вендор апи урл будет в таком формате

http://127.0.0.1:3000/vendor/api/v2 и мы будем его использовать везде где нам надо а не фейково его клеить например тут. Тогда весь наш контроллер вендора должен быть на этом пути оснвоной, а все его методы уже по /status и тд. Тогда у нас все станет логичнее а проверка в /status в ошибочном варианте который мы сломаем будет захардкожена ссылка и там мы выставим v1 это надо будет только указать в scenarios/01-healthcheck-debug/SABOTAGE.md

а так у нас все вендоры будут хотдить по VENDOR_API_URL + их метод, так будет намного логичнее и понятнее, что сломано

4. так же можем немного оптимизировать названия контроллером освновных хэлсчеков если это нужно

5. После всего обнови README файлы и SABOTAGE.md
